"""Built-in behaviour plugins.

Every module here is auto-discovered by the :class:`PluginManager`. Plugins
shape how the userbot behaves (they hook the engine lifecycle); they do not add
Telegram commands — that is what modules are for.
"""
